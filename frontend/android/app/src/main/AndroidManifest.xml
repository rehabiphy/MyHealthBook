<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        <!-- Email verification deep link: myhealthbook://verify?token=...&email=...
             Bare custom scheme, no domain/App-Links hosting needed. -->
        <intent-filter android:autoVerify="false">
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.DEFAULT" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="myhealthbook" android:host="verify" />
        </intent-filter>
        <!-- Real Android App Link: https://myhealthbook.rehabiphy.com/verify?...
             autoVerify="true" makes Android check backend/public/.well-known/
             assetlinks.json on install and, once verified, open this app
             directly instead of a browser when the link is tapped — this is
             what makes the email's "Verify My Email" button work reliably
             (a bare custom scheme is stripped by some mail clients; a normal-
             looking https link isn't). -->
        <intent-filter android:autoVerify="true">
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.DEFAULT" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="https" android:host="myhealthbook.rehabiphy.com" android:pathPrefix="/verify" />
        </intent-filter>
      </activity>
    </application>
</manifest>
